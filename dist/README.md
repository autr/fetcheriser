FETCHING FETCHERISER FOR FETCHING FETCHED FETCHES
=================================================

```
npm i fetcheriser
```

```

await get( url, { ...args } )
await post( url, { ...args } )
await put( url, { ...args } )
await delete( url, { ...args } )

```